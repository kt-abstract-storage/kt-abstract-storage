package buildsrc.convention

// This precompiled plugin is intentionally kept as a no-op to preserve plugin ID compatibility
// while preventing buildSrc from forcing external plugin jars onto the root build classpath.
